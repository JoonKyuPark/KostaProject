<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="job.resume.mapper.BoardMapper">
	<cache />
	<insert id="insertResume" parameterType="Resume">
		insert into board2( !!!!!수정필요
			b_id, b_name, b_title, b_content, b_pwd, b_hit, b_date, b_ref, b_step, b_level, b_fname
			) values(
				#{b_id}, #{b_name}, #{b_title}, #{b_content}, #{b_pwd}, 0, SYSDATE, #{b_ref}, #{b_step}, #{b_level}, #{b_fname}
			)		
	</insert>
	
	
	

</mapper>











